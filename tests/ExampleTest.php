<?php
use PHPUnit\Framework\TestCase;

class ExampleTest extends TestCase
{
    public function testExample()
    {
        $this->assertTrue(true);
    }
}

// Pour utiliser PHPUnit, installez-le via Composer avec la commande : composer require --dev phpunit/phpunit
// Pour exécuter les tests, utilisez la commande : vendor/bin/phpunit tests
// Les assertions courantes incluent : assertTrue, assertFalse, assertEquals, assertCount, etc.
?>